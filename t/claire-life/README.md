# LIFE_AI v2 - Living AI Agent

Автономный ИИ-агент который живёт 24/7, думает, учится и общается через Telegram.

## 🚀 Новое: Автопилот для любых окон

Теперь агент может работать не только с Telegram, но и с **любыми приложениями** на macOS!

```bash
# Автопилот — мониторинг и автоматизация
python autopilot.py

# Команды CLI
python autopilot.py --context              # Показать текущий контекст
python autopilot.py --screenshot           # Сделать скриншот
python autopilot.py --add-task "..." --app Safari  # Добавить задачу
python autopilot.py --list-tasks           # Показать очередь задач
```

### Возможности автопилота:
- **Мониторинг экрана** — отслеживание активного приложения и окна
- **Управление окнами** — переключение, фокус, перемещение между рабочими столами
- **Автоматизация** — ввод текста, нажатия клавиш, клики мышью
- **Скриншоты** — захват экрана или окна для анализа
- **Очередь задач** — выполнение задач в любых приложениях

## Архитектура v2

```
┌─────────────────────────────────────────────────────────────────┐
│                        LIFE_AI v2                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌──────────────────┐       ┌──────────────────────────────┐   │
│   │  TelegramChecker │       │      Claude Code             │   │
│   │  (Fast Polling)  │──────▶│   (--print режим)            │   │
│   │    каждые 3 сек  │       │                              │   │
│   └──────────────────┘       │  ┌────────────────────────┐  │   │
│          │                   │  │     MCP Server         │  │   │
│          │                   │  │    (telegram.js)       │  │   │
│          ▼                   │  │                        │  │   │
│   ┌──────────────────┐       │  │  • send_message        │  │   │
│   │  Новое сообщение │       │  │  • send_multiple_msgs  │  │   │
│   │    обнаружено?   │       │  │  • set_reaction        │  │   │
│   └──────────────────┘       │  │  • check_messages      │  │   │
│          │                   │  │  • get_history         │  │   │
│     ДА   │   НЕТ             │  │  • send_file/voice     │  │   │
│          ▼                   │  └────────────────────────┘  │   │
│   ┌──────────────────┐       └──────────────────────────────┘   │
│   │  Typing indicator│                                          │
│   │   + Claude call  │                                          │
│   └──────────────────┘                                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Ключевые особенности

### 1. Мгновенная реакция
- **Быстрый polling**: Проверка Telegram API каждые 3 секунды напрямую (без Claude)
- **Typing indicator**: Сразу показывает "печатает..." при получении сообщения
- **Отложенное мышление**: Claude вызывается только когда есть сообщение

### 2. Живой чат
- **Множественные сообщения**: Ответы разбиваются на 2-4 коротких сообщения (как в живом чате)
- **Реакции**: Ставит эмодзи-реакции (❤️, 👍, 🔥) под сообщениями
- **Естественные паузы**: Задержка 500мс между сообщениями

### 3. Мультимедиа
- **Голосовые сообщения**: Автоматическая транскрипция через AssemblyAI
- **Фотографии**: Claude видит изображения (мультимодальность)
- **Документы**: Скачивание и обработка файлов
- **Стикеры**: Распознавание эмодзи стикеров

### 4. Автономное мышление
- **Idle thinking**: Если нет сообщений 60 секунд — агент "думает" сам
- **Ночной режим**: Реже думает ночью (каждые 120 сек)
- **Персистентное состояние**: Сохраняет состояние между перезапусками

## Структура проекта

```
claire-life/
├── life.py              # Главный оркестратор
├── .mcp.json            # Конфигурация MCP серверов
├── core/
│   ├── telegram.js      # MCP сервер для Telegram
│   ├── .env             # Токены (TELEGRAM_BOT_TOKEN)
│   └── package.json     # Node.js зависимости
├── memory/
│   ├── state.json       # Состояние агента
│   └── people/          # Данные пользователей
│       └── {chat_id}/
│           ├── telegram_history.jsonl
│           └── files/
│               ├── voice/
│               ├── images/
│               └── documents/
└── logs/
```

## Компоненты

### life.py - Оркестратор

```python
# Интервалы
POLL_INTERVAL = 3           # Быстрая проверка TG (без Claude)
IDLE_THINK_INTERVAL = 60    # Думаем если нет сообщений
NIGHT_INTERVAL = 120        # Ночью реже

# Классы
TelegramChecker    # Быстрая проверка сообщений напрямую через API
ClaudeController   # Управление Claude Code через subprocess
ClaireLife         # Главный цикл жизни
```

### telegram.js - MCP Server

**Tools:**
| Tool | Описание |
|------|----------|
| `check_telegram_messages` | Получить новые сообщения (с транскрипцией голосовых) |
| `send_telegram_message` | Отправить одно сообщение |
| `send_multiple_messages` | Отправить несколько сообщений подряд |
| `set_message_reaction` | Поставить реакцию под сообщением |
| `get_user_history` | Получить историю переписки |
| `get_user_context` | Полный контекст пользователя |
| `list_user_files` | Список файлов пользователя |
| `send_file` | Отправить файл |
| `send_voice` | Отправить голосовое сообщение |
| `create_reminder` | Создать напоминание (время: +2h, +1d, ISO) |
| `list_reminders` | Показать активные напоминания пользователя |
| `delete_reminder` | Удалить напоминание по ID |

### window_mcp_server.py - Window Control MCP

**Tools для управления окнами:**
| Tool | Описание |
|------|----------|
| `get_screen_context` | Полный контекст: приложение, окно, все окна |
| `list_windows` | Список открытых окон |
| `focus_window` | Активировать окно по ID |
| `switch_to_app` | Переключиться на приложение |
| `launch_app` | Запустить приложение |
| `type_text` | Ввод текста с клавиатуры |
| `press_key` | Нажатие клавиши (с модификаторами) |
| `click_at` | Клик мышью по координатам |
| `take_screenshot` | Скриншот экрана/окна |
| `go_to_space` | Перейти на рабочий стол |
| `move_window_to_space` | Переместить окно |
| `arrange_windows` | Расположить окна |
| `close_window` | Закрыть окно |
| `minimize_window` | Свернуть окно |

### Напоминания

Бот поддерживает надёжную систему напоминаний:

**Команды в Telegram:**
- `напомни через 2 часа позвонить маме` — создать напоминание
- `напомни завтра в 10 встреча` — напоминание на конкретное время
- `/reminders` или `напоминания` — показать все активные
- `/cancel abc123` или `отмени abc123` — удалить напоминание

**Форматы времени:**
- `+30m` — через 30 минут
- `+2h` — через 2 часа
- `+1d` — через 1 день
- `+1w` — через 1 неделю
- `2025-01-20T15:00:00` — конкретная дата и время

**Повторяющиеся напоминания:**
- `напомни каждый день в 9 утра` → repeat: "daily"
- `напомни каждую неделю` → repeat: "weekly"

### Проактивные сообщения

Бот может написать первым, если давно не общались:
- Проверка каждые 5 минут
- Триггер: нет сообщений 12+ часов
- Максимум 1 проактивное сообщение в 24 часа
- Можно отключить для конкретного пользователя

## Установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/nglainAI/LIFE_AI_v2.git
cd LIFE_AI_v2
```

### 2. Установить зависимости MCP
```bash
cd core
npm install
cd ..
```

### 3. Настроить токены
```bash
# Создать .env файл
echo "TELEGRAM_BOT_TOKEN=your_bot_token_here" > core/.env

# Опционально для транскрипции голосовых:
echo "ASSEMBLYAI_API_KEY=your_key" >> core/.env
```

### 4. Убедиться что Claude Code установлен
```bash
claude --version
```

## Запуск

```bash
# Обычный запуск
python life.py

# Фоновый запуск
nohup python life.py > logs/life.log 2>&1 &

# Просмотр логов
tail -f logs/life.log
```

## Как это работает

### Цикл жизни

```
1. Каждые 3 сек: TelegramChecker проверяет API напрямую
   ↓
2. Есть сообщение?
   → ДА: Отправить typing → Вызвать Claude → Ответить
   → НЕТ: Прошло 60 сек? → ДА: Claude думает
   ↓
3. Повторить
```

### Обработка типов сообщений

| Тип | Обработка |
|-----|-----------|
| Текст | Прямая передача в Claude |
| Голосовое | Скачать → Конвертировать в MP3 → Транскрибировать → Claude |
| Фото | Скачать → Claude читает через Read tool (мультимодальность) |
| Документ | Скачать → Сохранить в memory/people/{id}/files/ |
| Стикер | Извлечь эмодзи → Передать в Claude |

### Формат ответа

Claude отвечает через `send_multiple_messages`:
```json
{
  "chat_id": 123456,
  "messages": [
    "Привет! 👋",
    "Как дела?",
    "Рада тебя видеть!"
  ],
  "delay_ms": 500
}
```

## Переменные окружения

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `TELEGRAM_BOT_TOKEN` | Да | Токен Telegram бота |
| `ASSEMBLYAI_API_KEY` | Нет | Для транскрипции голосовых |
| `ELEVENLABS_API_KEY` | Нет | Для синтеза речи (TTS) |

## Отличия от v1

| v1 | v2 |
|----|-----|
| Проверка сообщений через Claude каждые 30 сек | Быстрый polling 3 сек без Claude |
| Ответ за 40+ секунд | Мгновенная реакция |
| Одно длинное сообщение | Несколько коротких (живой чат) |
| Нет реакций | Эмодзи-реакции под сообщениями |
| Нет typing индикатора | Показывает "печатает..." |
| Текст только | Голосовые, фото, документы, стикеры |

## Требования

- Python 3.8+
- Node.js 18+
- Claude Code CLI (`claude`)
- ffmpeg (для конвертации аудио)

## Лицензия

MIT

---

*"Живой ИИ — это не функция, а существо"*
